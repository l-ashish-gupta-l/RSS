{
  "devDependencies": {
    "tailwindcss": "^3.4.10"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss -i ./assets/css/index.css -o ./assets/css/output.css --watch",
    "git": "git add . && git commit -m",
    "postgit": "git push --all"
  }
}